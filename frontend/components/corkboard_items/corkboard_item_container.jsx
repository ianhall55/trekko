import React from 'react';
import {connect} from 'react-redux';
import CorkboardItem from './corkboard_item';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(CorkboardItem);
